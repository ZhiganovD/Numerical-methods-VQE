1.- USE LINUX (or Mac OS, but idk about it)
    - To use Linux in Windows, I installed Linux (Ubuntu): 
      - Open Powershell as Administrator
      - Run: wsl --install
      - To open Linux, type in Windows Search: Ubuntu, and open it.

2.- INSTALLING CONDA (and VsCode)
    -  Open Firefox (run: firefox)
    -  Go to https://repo.anaconda.com/archive/
        -  Download Anaconda3-2025.06-1-Linux-x86_64.sh (for example in Downloads)
    -  Go to https://code.visualstudio.com/updates/v1_106
        -  Download the .deb file
    -  Run: cd Downloads
        - (to check files in Downloads, run: ls)
    -  Run: bash Anaconda3-2025.06-1-Linux-x86_64.sh
    -  Run: sudo apt install ./code_1.106.2-1763572306_amd64.deb


            Next steps may be not necessary, but I ran them, so just in case.
    -  Run: echo "export PATH=$PATH:/home/USERNAME/anaconda3/bin">> ~/.bashrc     #replace USERNAME with your username
    -  Run: nano ~/.bashrc

3.- CREATING CONDA ENVIRONMENT 
    -  Run: conda create --name vqe-jupyter python=3.12.7      #vqe-jupyter is an example name, create it simple to remember it
    
4.- ACCESSING CONDA ENVIRONMENT AND INSTALLING PACKAGES
    - Run: conda activate vqe-jupyter                      #RUN THIS EACH TIME YOU OPEN UBUNTU OR TURN ON YOUR COMPUTER
        (if you dont remember the name, run: conda env list)
    - Run: (in this order)
        conda install matplotlib=3.10.6
        conda install conda-forge::pennylane=0.37.0
        conda install conda-forge::pyscf=2.10.0
        conda install conda-forge::pylatexenc=2.10
        conda install ipykernel=6.30.1
        conda install conda-forge::qiskit-ibm-runtime=0.43.1
        pip install qiskit-ibm-catalog==0.10.0
        conda install conda-forge::qiskit-aer=0.17.2
        pip install qiskit-nature==0.7.2
        conda install anaconda::scikit-learn=1.7.2
        pip install qiskit-ibm-catalog==0.11.0
        pip install qiskit-serverless==0.27.1

5.- ADDING SPECIFIC FIX BECAUSE OF PACKAGES ISSUES
    - Close and open Ubuntu
    - Run: cd /home/gregor/anaconda3/envs/vqe-jupyter/lib/python3.12/site-packages/qiskit/primitives     #replace gregor (and vqe-jupyter if different)
    - Run: code __init__.py
    - Go almost to the bottom of the file, just between
                    .. autosummary::
                       :toctree: ../stubs/
                    
                       BaseSamplerV1
                       SamplerResult
                    
                    """

        and

                    from .base import (
                        BaseEstimatorV1,
                        BaseEstimatorV2,
                        BaseSamplerV1,
                        BaseSamplerV2,
                    )


    - There, insert this code:
###CHANGE
# Compatibility shim for packages expecting the old `Estimator` class.
# Qiskit 2.x removed Estimator and replaced it with EstimatorV2.
# This alias allows qiskit-nature <= 0.7.2 and qiskit-algorithms <= 0.4.0
# to continue working without modifying their source code.
  
try:
    from .base import BaseEstimatorV2 as BaseEstimator
except Exception:
    # If BaseEstimatorV2 isn't available, create a dummy class so imports succeed.
    class BaseEstimator:
        pass
  
### END OF CHANGE

    - Save the file


6.- CREATING GIT CLONE REPOSITORY IN COMPUTER	
	- Open VsCode
	- Log in in VsCode (bottom left) with the email of github
	- On left pannel, click: Source Control symbol
	- Click: Clone Repository
	- Paste: https://github.com/ZhiganovD/Numerical-methods-VQE.git
	- Choose a folder to store the repository
	

7.- PUSHING LOCAL CHANGES TO THE WEB REPOSITORY
	- Save file in VsCode
	- Click: Source Control symbol
	- Click: '+' symbol on the right of Changes
	- Add a message (eg: "commit algorithm change") and click: Commit
	- Add a messag (eg: "sync algorithm change") and click: Sync Changes
	
	


EVERYTIME:
    - To open VsCode: run: code (if first time, install recommended extensions like python and jupyter that appear automatically recommended)




